{
  "name": "codemux",
  "version": "0.1.0",
  "description": "Unified CLI for AI coding agents",
  "license": "MIT",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "codemux": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test",
    "smoke": "bun run src/index.ts --help >/dev/null && bun run src/index.ts --version && bun run src/index.ts verify --show-scode >/dev/null",
    "link": "bun link",
    "unlink": "bun unlink"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "js-yaml": "^4.1.1"
  }
}
